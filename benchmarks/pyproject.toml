# Copyright 2026 The PyAthena authors
#
# Licensed under the MIT License.
# See LICENSE or https://opensource.org/licenses/MIT.
#
# SPDX-License-Identifier: MIT

[project]
name = "pyathena-benchmarks"
version = "0.1.0"
description = "Reproducible Athena cursor measurements"
requires-python = ">=3.12,<3.13"
classifiers = ["Private :: Do Not Upload"]
dependencies = [
    "PyAthena[pandas,arrow,polars]",
    "awswrangler>=3.17,<4",
    "polars>=1.37",
    "psutil>=7,<8",
]

[tool.uv]
package = false

[tool.uv.sources]
pyathena = { path = "..", editable = true }

[dependency-groups]
dev = ["pytest>=8", "pytest-asyncio>=1", "mypy>=1.19", "types-psutil", "cfn-lint>=1"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"

[tool.ruff]
extend = "../pyproject.toml"
exclude = [".venv"]
target-version = "py312"

[tool.mypy]
python_version = "3.12"
check_untyped_defs = true
disallow_untyped_defs = true
ignore_missing_imports = true
follow_imports = "silent"
files = ["pyathena_bench"]
